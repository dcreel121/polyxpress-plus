peDesigner (Author Directory):

Implements:

* Bower module management
* Grunt managed file copying, updating, jshint, minification
* Inject.js module loading.
* Unit testing (JSTestDriver) with full inject.js compatability.
* Uniform javascript module format

ToDo:

*  (DONE) Could cleanup javascript a bit more.  Improve modules. (low)
*  (DONE) Could decouple javascript from html (low)
*  Add more unit tests
*  (DONE) check that jquery, jquery.mobile are coming from CommonPX and not web
*  Usability testing

* CI Integration
* peDesignerController Tests
* peDesignerModel Tests
* peDesignerServer Tests

Notes:

** peModel
* Expected behavior for not finding an item